name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: terrymon-webapp/package-lock.json

      - name: Install dependencies
        working-directory: terrymon-webapp
        run: npm ci

      - name: Lint
        working-directory: terrymon-webapp
        run: npm run lint

      - name: Build
        working-directory: terrymon-webapp
        run: npm run build
